info:
  name: deleteTag
  type: http
  seq: 5
  tags:
    - Tags

http:
  method: DELETE
  url: "{{baseUrl}}/tags/:tagId"
  params:
    - name: tagId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: Tag deleted
    request:
      url: "{{baseUrl}}/tags/:tagId"
      method: DELETE
      params:
        - name: tagId
          value: ""
          type: path
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
