info:
  name: updateTag
  type: http
  seq: 4
  tags:
    - Tags

http:
  method: PATCH
  url: "{{baseUrl}}/tags/:tagId"
  params:
    - name: tagId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Updated tag
    request:
      url: "{{baseUrl}}/tags/:tagId"
      method: PATCH
      params:
        - name: tagId
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "name": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "name": "",
            "createdAt": ""
          }
