info:
  name: updateProject
  type: http
  seq: 4
  tags:
    - Projects

http:
  method: PATCH
  url: "{{baseUrl}}/projects/:projectId"
  params:
    - name: projectId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": "",
        "archived": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Updated project
    request:
      url: "{{baseUrl}}/projects/:projectId"
      method: PATCH
      params:
        - name: projectId
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "name": "",
            "archived": false
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "name": "",
            "archived": false,
            "createdAt": "",
            "updatedAt": ""
          }
