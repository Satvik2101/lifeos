info:
  name: deleteTask
  type: http
  seq: 5
  tags:
    - Tasks

http:
  method: DELETE
  url: "{{baseUrl}}/tasks/:taskId"
  params:
    - name: taskId
      value: c319f5ed-afab-48c4-875f-15c27a7ef7f4
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: Task deleted
    request:
      url: "{{baseUrl}}/tasks/:taskId"
      method: DELETE
      params:
        - name: taskId
          value: ""
          type: path
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
